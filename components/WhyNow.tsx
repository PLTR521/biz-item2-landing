export default function WhyNow() {
  return (
    <section className="border-t border-[var(--border)] px-6 py-16 md:py-24">
      <div className="mx-auto max-w-3xl">
        <h2 className="mb-4 text-2xl font-semibold tracking-tight md:text-3xl">
          Why now
        </h2>
        <p className="text-lg leading-relaxed text-[var(--text-secondary)]">
          AI agents are starting to send more email than the humans behind them.
          Deliverability tools still assume every sender is a person. That&apos;s
          the gap we&apos;re building for.
        </p>
      </div>
    </section>
  );
}
